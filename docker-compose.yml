services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: orchestai
      POSTGRES_USER: orchestai
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U orchestai"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
